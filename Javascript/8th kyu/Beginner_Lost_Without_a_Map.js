/*
Given an array of integers, return a new array with each value doubled.

For example:

[1, 2, 3] --> [2, 4, 6]
*/

//answer
function maps(x) {
    //Returns a array x back with every element doubled
    return x.map(i => i * 2)
}