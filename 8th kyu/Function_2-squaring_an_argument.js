/*
Now you have to write a function that takes an argument and returns the square of it.
*/

//answer    
//simple function that takes in a number and returns it's square value
const square = num => Math.pow(num,2)
