/*
This function should return an object, but it's not doing what's intended. What's wrong?
*/

//answer    
function mystery() {
    var results =
        { sanity: 'Hello' }
    //brought result inline to return   
    return results
}