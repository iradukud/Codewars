/*
Complete the function which converts a binary number (given as a string) to a decimal number.
*/

//answer    
//converts a biinary string number to a decimal number
const binToDec = bin => parseInt(bin,2)